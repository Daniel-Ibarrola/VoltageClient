import { expect, describe, it, vi } from "vitest";
import { SearchForm } from "./SearchForm.jsx";

describe("SearchForm", () => {
    it("calls on search submit on button click", () => {
        expect(false).toBe(true);
    });
});
